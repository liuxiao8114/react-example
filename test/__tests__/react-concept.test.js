import x from '../../react-concept.js'

const TEST_USER_1 = {
  firstname: 'Xiao',
  lastname: 'Liu'
}

describe('promise test cases', () => {
  it('one test', () => {
    function foo() {
      let a, b, c
    }
  })
})
